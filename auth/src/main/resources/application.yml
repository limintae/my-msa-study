server:
  port: 8083

spring:
  application:
    name: auth-service
  profiles:
    active: local

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8081/eureka/
